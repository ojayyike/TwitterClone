extends layouts/login-layout.pug 

block content
    .loginContainer
        h1 Login 
        form(method="post") 
            input#userName(type="text", name="logUsername", placeholder ="Username or email", required="")
            input#userName(type="password", name="logPassword", placeholder ="Password", required="")
            input(type="submit", value="Login")
            a(href="/register") Need an Account? Register Here.